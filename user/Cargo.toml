[package]
name = "user_lib"
version = "0.1.0"


[dependencies]
riscv = { git = "https://gitee.com/rcore-os/riscv", features = ["inline-asm"] }
[profile.release]
debug = true

# [features]
# board_qemu = []
# board_k210 = []